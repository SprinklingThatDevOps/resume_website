version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 22.22.2
        - nvm use 22.22.2
        - node --version
        - npm install -g pnpm@11.7.0
        - pnpm --version
        - pnpm config set store-dir .pnpm-store
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .pnpm-store/**/*
      - node_modules/**/*
